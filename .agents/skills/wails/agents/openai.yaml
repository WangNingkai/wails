interface:
  display_name: "Wails"
  short_description: "Repo-specific patterns and workflows for wails"
  default_prompt: "Use the wails repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true